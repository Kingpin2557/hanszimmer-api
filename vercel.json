{
  "version": 2,
  "builds": [
    {
      "src": "/server/app.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["server/routes.ts", "server/routes/**", "server/data/**"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/server/app.ts" }]
}
